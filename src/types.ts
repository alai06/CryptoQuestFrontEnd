/**
 * Types partagés à travers toute l'application.
 * Centralise les définitions pour éviter les redéclarations.
 */

export type Screen = 'home' | 'tutorial' | 'solver' | 'generator' | 'game' | 'progress';
